function [owensHeight,owensWidth] = drawOwens (imageName)

 owenSize = 50;

clf;

figure(1);

% read the background image
image = imread(imageName);

% get the size of the image
  [owensHeight,owensWidth] = ;

% display the image
imshow(imageName);



endfunction
